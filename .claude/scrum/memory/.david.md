# David's notes — agentic-memories

## Stack
- Python 3.12, FastAPI, ChromaDB, TimescaleDB, Redis. Frontend: React/Vite (port 3003), Docker Compose.
- API on 8080, ChromaDB on 8000, TimescaleDB on 5432, Redis on 6379.

## Build / test commands
- `make test` — pytest unit + integration (no e2e, no Docker required for these targets).
- `make test-fast` — `pytest tests/unit -q`.
- `make test-unit` / `make test-integration` for finer slices.
- `make test-e2e` and `make test-all` need Docker stack up via `make start`.
- `make lint` / `make format` for ruff.
- pytest config in `pytest.ini`.

## Code layout I rely on
- Entry points
  - `src/app.py` — main FastAPI app, mounts routers including `routers/memories.py`.
  - `src/routers/memories.py` — `POST /v1/memories/direct` (`store_memory_direct`, sync def at `:321`), `DELETE /v1/memories/{memory_id}`. Embedding called inline at `:363`.
  - `src/services/retrieval.py:70` — `search_memories(user_id, query, ...)`. Embedding at `:139` only when query is non-empty.
  - `src/services/hybrid_retrieval.py:97` — `HybridRetrievalService.retrieve_memories(query)`. Today already wraps via `tracing.start_span("hybrid_retrieval", ...)` at line 109; embedding for semantic path at `:173`.
  - `src/services/episodic_memory.py:44` — `EpisodicMemoryService.store_memory(memory)`; embedding inside `_store_in_chroma` (`:113-145`).
  - `src/services/episodic_memory.py:195` — `retrieve_memories(...)` → `_semantic_search` (`:225-275`); embedding at `:236`.
  - `src/services/emotional_memory.py:184` — `EmotionalMemoryService._store_in_chroma(memory)`; embedding at `:196`.
  - `src/services/compaction_graph.py:944` — `run_compaction_graph(user_id, ...)`. Used to call `tracing.start_trace("compaction_job", ...)` at `:964` then `trace.update(...)` at `:993` — never closed (span leak). Story 2.1 rewrites it as `with langfuse_client.start_as_current_observation(...)`.
  - `src/services/compaction_ops.py:74` — `simple_deduplicate(user_id, ...)`; embedding inside the loop at `:137` (only when stored embedding is missing).
- Langfuse helpers
  - `src/dependencies/langfuse_client.py` — `get_langfuse_client()` singleton (returns None if disabled or import fails).
  - `src/services/tracing.py` — wraps Langfuse v3 SDK with contextvars (`_current_root_span`, `_span_stack`). `start_span` already calls `start_as_current_observation`, so once a root exists it nests correctly via OTEL contextvars.
  - Canonical wrap pattern (do not modify): `src/services/unified_ingestion_graph.py:1428-1452`.
  - Fallback pattern when Langfuse unavailable / import fails: `unified_ingestion_graph.py:1468-1480`.

## Langfuse SDK (v3 / langfuse==3.14.2) — usage rules
- Wrap form (sync): `with client.start_as_current_observation(as_type="span", name=..., trace_context={"trace_id": ..., "user_id": ..., "session_id"?: ...}, input=..., metadata=...) as root_span: ...`.
- Generate trace ids with `client.create_trace_id()` (don't fabricate).
- Detect if a parent span is already active: `client.get_current_trace_id()` returns `Optional[str]` (None if no active span). There is **no** `get_current_span()` in v3.
- On success: `root_span.update(output={...small summary...})`. Keep payloads small — counts, ids, latencies. **No** embedding vectors, **no** full retrieval bodies.
- On exception: just let the `with` exit; the context manager closes the span. Re-raise (or let it propagate).
- Always degrade gracefully: import errors / client failures must not break the underlying call. Mirror `unified_ingestion_graph.py:1468-1480`.

## Conventions / gotchas
- Existing `tracing.start_span(...)` calls in `compaction_graph.node_*` and elsewhere are **child** spans only — they need a root span in the contextvar to attach to. Once we create a real `start_as_current_observation` root via the singleton client, those nested `start_span` calls (which read `_current_client`/`_current_root_span` from `tracing.start_trace`) will NOT auto-attach unless we also call `start_trace`. BUT they'll still chain via OTEL because `start_span` itself uses `client.start_as_current_observation` which inherits OTEL context. So nested `start_span` calls in the graph body will attach as OTEL children of our outer wrap even though the `_current_root_span` contextvar isn't populated. Verified by reading `tracing.py` and the SDK pattern.
- For compaction: replacing `start_trace` with `with langfuse_client.start_as_current_observation(...)` means `_current_root_span` is no longer set by `tracing.py`, so `start_span` (which checks `_current_client.get()` and `_current_root_span.get()`) returns None for nested spans. Workaround: also populate `_current_client` and `_current_root_span` so existing nested spans keep working — OR migrate them. Cheapest = populate the contextvars manually around the with block.
- Direct ingest endpoint: `POST /v1/memories/direct` (router prefix `/v1/memories` + route `/direct`).
- `forget.py:66` is dead code (Parminder confirmed) — `simple_deduplicate` in that file has no callers.
- Always use the singleton via `from src.dependencies.langfuse_client import get_langfuse_client`. Don't instantiate `Langfuse(...)` directly except in `unified_ingestion_graph.py` (existing smell, separate story).

## Testing
- Run `make test` after each change. Targeted: `pytest tests/unit/services/test_episodic_memory.py -v`.
- E2E require docker; skip unless explicitly asked.
