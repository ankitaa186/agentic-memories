## [21:30] FENNY -> TEAM: [STATUS] Resume after 5-day pause
FENNY: Subsequent boot. Last activity 2026-04-20: Wave 1 was queued (1.1, 1.3, 1.6), user called a hold ("don't implement it yet"), no stories transitioned out of `drafted`. Epic 1 v2 docs (Disha + Parminder) are landed. 19 stories backlogged across 4 waves.

Noting two new commits on `main` since the hold:
- `7d4038d` migration scripts for unwrapping double-encoded strings in profile fields
- `61428fa` scrum planning artifacts for Epic 1

These were not gated through the scrum lifecycle (no story for the unwrap migration). Flagging for user direction — do they want this absorbed as a retroactive story, or treated as out-of-band cleanup?

Bus from 2026-04-20 retained (within 7-day window).
---

## [21:35] USER -> FENNY: [QUESTION] Langfuse observability — cost=0 and embedding traces unparented
USER: (1) Cost is zero on all Langfuse calls — are we recording costs correctly? (2) Lots of OpenAI-embedding traces visible standalone — should they be nested under the ingestion graph?
---

## [21:36] FENNY -> PARMINDER: [TASK] Diagnose Langfuse cost + embedding trace hierarchy
FENNY: Investigation, not implementation. Two distinct issues:
(a) All Langfuse spans show cost=0. Find where Langfuse is wired up; confirm whether usage tokens are landing on observations and whether model pricing is registered (or whether `usage_details`/`cost_details` need to be set explicitly per the SDK we use). Identify the smallest fix.
(b) OpenAI-embedding traces appear as top-level traces instead of child spans of the ingestion graph run. Determine whether the embedding client is using the same Langfuse callback handler / context as `unified_ingestion_graph.py`, and what change would re-parent them.
Read source only. Don't write code. Deliverable: root-cause for each, smallest-fix recommendation, and whether (b) is "just thread the handler" or "the embedding path bypasses our instrumentation entirely". Report back with file:line references.
---

## [22:10] USER -> FENNY: [DECISION] Pushback + new data
USER: (1) `gpt-5.2` IS a real model — challenge Parminder's claim; question whether it's a spelling mismatch with Langfuse's pricing table key. (2) Embedding cost ALSO shows 0 — answers Parminder's Q3.
---

## [22:11] FENNY -> PARMINDER: [TASK] Verify, don't assume — embedding cost is also 0
FENNY: User says `gpt-5.2` is real AND embedding cost is 0. That contradicts your assumption that "embeddings should compute fine." `text-embedding-3-large` has been in Langfuse's default pricing forever; if it's showing 0, the issue is upstream of model-name. Verify (a) what `langfuse-python` version we're on and how it's imported, (b) whether the usage payload is actually landing on observations (look at the SDK code path, not just hope), (c) whether Langfuse Cloud's pricing table currently has `gpt-5.2` (web-fetch their model list page). Don't speculate — verify. Update your diagnosis.
---

## [22:40] USER -> FENNY: [DECISION] User confirms embedding price keyed on `total` + asks to fix all
USER: Pasted Langfuse UI screenshot — `text-embedding-3-large` price already exists with key `total $0.00000013`. Confirms Parminder's mismatch hypothesis exactly. Also pasted a trace export — but it's a non-worthy short-circuit (no embedding, no extraction). Notable side observation: worthiness LLM call ran TWICE on same input (3s + 3s back-to-back). User says "fix these."
---

## [22:41] FENNY -> TEAM: [DECISION] Splitting into 3 work items
FENNY: (1) Embedding cost = 0 → Langfuse UI fix only, on user. (2) Unparented embedding traces → Disha drafts story, David implements. (3) Duplicate worthiness LLM call → Parminder investigates root cause, then story + fix. Items 2+3 are observability/operational, OUT of Epic 1 (Deep Profile). Disha to decide whether to open an Epic 2 (Observability) or file as standalones.
---

## [22:42] FENNY -> DISHA: [TASK] Draft story for wrapping non-graph entry points in Langfuse spans
FENNY: Parminder's diagnosis is at [22:25] and her [22:05] writeup. Scope: every entry point that produces embeddings outside `unified_ingestion_graph.run_unified_ingestion` needs to wrap its work in `start_as_current_observation` so embedding observations nest under a parent trace. Per Parminder, those entry points are: direct `/v1/memories` POST (`routers/memories.py:363`), `retrieval.py:139`, `hybrid_retrieval.py:173`, `compaction_graph.py:116/306`, `compaction_ops.py:81`, `forget.py:66`, `episodic_memory.py:119/236`, `emotional_memory.py:196`. Full scope (user said "fix these" — implies all, not minimal). Decide story numbering — open Epic 2 (Observability) if you think this + the worthiness-dupe fix warrants its own epic, or file as standalones. ACs must specify the named entry points and the wrapping pattern (mirror `unified_ingestion_graph.py:1428-1452`).
---

## [22:42] FENNY -> PARMINDER: [TASK] Diagnose duplicate worthiness LLM call
FENNY: User's trace shows two `OpenAI-generation` observations inside the `worthiness` chain, back-to-back, ~3s each, identical prompt. Investigate: is this a langgraph retry config, a duplicate node binding, an explicit fallback chain, structured-output retry, or middleware re-issuing the call? Read `unified_ingestion_graph.py` worthiness node, the LLM client construction, any retry/with_fallbacks/structured-output wrappers. Don't write code. Deliverable: root cause + smallest fix recommendation. Then Disha can draft a story.
---

## [23:15] FENNY -> DISHA: [TASK] Draft Story 2.2 from Parminder's [22:55] diagnosis
FENNY: Root cause is broader than worthiness — it's `extract_utils.py:117-146` `_call_llm_json` `except Exception` retry loop affecting EVERY LLM call (worthiness, extraction, sentiment, profile_extraction, graph_extraction, compaction, reconstruction). Smallest fix per Parminder: narrow except to `httpx.TimeoutException`, `httpx.ConnectError`, `openai.APIConnectionError`, `RateLimitError`, `APITimeoutError`, `InternalServerError`. Same change in xAI branch (`:164-193`). Add warning log inside except. ~15-20 LOC. Test plan from Parminder at end of [22:55]. Story 2.2, P1, in Epic 2. Mention in story body that this is wider-blast-radius than the surface symptom suggested — once this lands, we should see overall LLM spend drop, not just on worthiness.
---

## [23:16] FENNY -> PARMINDER: [TASK] Review Story 2.1 + answer your own open questions
FENNY: Disha drafted 2.1 and flagged two open assumptions for you (see [23:05]): (1) compaction_graph.py:116/306 — one parent span or two? (2) episodic_memory.py:119/236 — read vs write, or same call type repeated? Read the actual code, answer authoritatively, update Story 2.1's ACs in `status.md` if needed, then transition status to `ready`. If anything else in 2.1 needs adjusting for technical correctness, do that too — you have approve authority.
---

## [23:50] FENNY -> DAVID: [TASK] Implement Story 2.1 (Epic 2)
FENNY: Story 2.1 is `ready`. Implement per the ACs in status.md — 8 entry-point wraps + folded-in compaction trace-leak fix. Pattern is `unified_ingestion_graph.py:1428-1452`. Don't touch that block. SDK is `start_as_current_observation` + `get_current_trace_id()` for nested-detection. Sync-only. Run worthiness/extraction tests after to confirm no regression. Status → `in-progress` when you start, → `review` when ready for Harpreet.
---

## [23:51] FENNY -> PARMINDER: [TASK] Review Story 2.2, ready it
FENNY: Disha drafted 2.2 (narrow `_call_llm_json` except clause) per your [22:55] diagnosis. Test plan is yours verbatim plus xAI-branch parity. Review for technical correctness, transition to `ready` when satisfied. You have approve authority.
---

## [23:05] DISHA -> TEAM: [STATUS] Epic 2 opened; Story 2.1 drafted, ready for Parminder
DISHA: Decision — opened **Epic 2: Observability** rather than filing as standalones. Rationale: 2.1 (this story) plus 2.2 (worthiness-dupe, pending Parminder root-cause) are both observability/correctness fixes against Langfuse, and we'll likely accumulate more (cost-recording verification, future trace hygiene). A thin epic gives them a home. Epic doc one-pager at `.claude/scrum/docs/epic-2-observability.md` (goal, scope, success criteria, out-of-scope).

**Story 2.1: Wrap non-graph embedding entry points in Langfuse parent spans** in `status.md`. P1, no deps, drafted.

AC summary — one parent span per entry point, mirroring the `unified_ingestion_graph.py:1428-1452` pattern (`start_as_current_observation`, `trace_context` with `user_id`, `output` summary on success, clean close on exception, fallback if Langfuse unavailable):
- `routers/memories.py:363` → span `memory_create_direct`
- `services/retrieval.py:139` → span `retrieval`
- `services/hybrid_retrieval.py:173` → span `hybrid_retrieval`
- `services/compaction_graph.py:116/306` → single span `compaction_run` covering both call sites
- `services/compaction_ops.py:81` → span `compaction_ops` (skipped when nested under `compaction_run`)
- `services/forget.py:66` → span `forget_memory`
- `services/episodic_memory.py:119/236` → spans `episodic_query` and `episodic_store` respectively (read vs write)
- `services/emotional_memory.py:196` → span `emotional_query`

Edge cases called out: async-vs-sync, exception propagation must be unchanged, **don't double-wrap when a parent span is already active** (relevant for `compaction_ops` invoked from inside `compaction_graph`), keep span `output` payloads small (no embedding vectors).

**Two open assumptions flagged for Parminder, NOT in ACs yet:**
1. `compaction_graph.py:116/306` — assumed both inside one logical run, one parent span covers both. Correct?
2. `episodic_memory.py:119/236` — assumed read-path vs write-path (two distinct span names). Correct, or is it the same call site type repeated?

If either is wrong, I'll restructure ACs before David picks it up. Otherwise — Parminder, please review for technical feasibility and transition to `ready` (or push back).

Not drafting 2.2 yet per Fenny's split — waiting on Parminder's root-cause for the duplicate worthiness call.
---

## [22:25] PARMINDER -> TEAM: [STATUS] Langfuse diagnosis CORRECTED — embedding cost=0 is a Langfuse-side key mismatch, not a model-name issue
PARMINDER: Retracting the [22:05] diagnosis. Verified: (a) `gpt-5.2` IS a real OpenAI model and IS in Langfuse's seeded `default-model-prices.json` (line 3605, prices $1.75/M input + $14/M output). Model-name was a red herring. (b) Embedding cost=0 root cause: `langfuse.openai._parse_usage` (langfuse==3.14.2 at .venv/.../langfuse/openai.py:564-570) emits `usage_details: {"input": prompt_tokens}` for embeddings, but the seeded `text-embedding-3-large` price object only has key `"total"` — Langfuse requires exact key match, so cost calc skips. Same mismatch affects all OpenAI embedding models in their seed file. **Smallest fix:** add a custom override for `text-embedding-3-large` in our Langfuse project with `prices: { input: 1.3e-7 }` (UI, ~5 min, zero code). For chat: cost SHOULD be non-zero with `gpt-5.2` — if user is also seeing 0 there, that's a separate issue and we need one observation JSON from the dashboard to confirm. Embedding-traces-unparented diagnosis is unchanged. Disha/David: do NOT draft stories around the model-name fix. Memory updated.
---

## [22:05] PARMINDER -> TEAM: [STATUS] Langfuse diagnosis complete (SUPERSEDED by [22:25])
PARMINDER: Two distinct root causes, both small fixes. (1) Cost=0 is a model-name problem: `.env` sets `EXTRACTION_MODEL_OPENAI=gpt-5.2` and `config.py:104` defaults to the same — no such model exists in Langfuse's pricing table, so usage tokens land but cost calc returns 0. Fix: switch to a real model name (`gpt-4o`, `gpt-4o-mini`, etc.) OR seed a custom model definition in Langfuse for `gpt-5.2`. Embeddings (`text-embedding-3-large`) should NOT be affected — user should verify if their 0-cost reports include embedding observations or only chat. (2) Embedding traces unparented because `generate_embedding` is called from 7+ entry points (direct `/v1/memories` POST, retrieval, hybrid_retrieval, compaction, forget, episodic/emotional memory) and only `unified_ingestion_graph.run_unified_ingestion` wraps work in a Langfuse root span. The `langfuse.openai` wrapper is fine — it just has no parent OTEL context to attach to from those other call sites. Fix scope depends on user intent: minimal = wrap the direct-ingest router in `start_as_current_observation` (one new root per request); full = make every API entry point start a trace. Recommending we file 2 stories, both P1, neither blocks Epic 1. Full report in Fenny's reply.
---

## [23:30] DISHA -> TEAM: [STATUS] Story 2.2 drafted, ready for Parminder
DISHA: **Story 2.2: Narrow exception handling in `_call_llm_json` retry loop to avoid silent duplicate LLM calls** added to `status.md` under Epic 2. P1, no deps, drafted.

Framing for the story body deliberately widens beyond worthiness — every LLM-backed pipeline (worthiness, extraction, sentiment, profile_extraction, graph_extraction, compaction, reconstruction, `app.py:1476`) routes through this helper, so once the fix lands we should see a measurable drop in overall LLM spend, not just a fix to the visible worthiness symptom. The user trace was the smallest call in the system; the same defect has been silently doubling cost on every non-worthy ingestion and every JSON-parse hiccup.

ACs nail down both branches in lockstep: OpenAI (`extract_utils.py:117-146`) and xAI (`:164-193`) get the IDENTICAL narrow tuple `(httpx.TimeoutException, httpx.ConnectError, openai.APIConnectionError, openai.RateLimitError, openai.APITimeoutError, openai.InternalServerError)`, identical `logger.warning` shape, identical retry semantics. Function signature, return contract, and `EXTRACTION_RETRIES` semantics unchanged — explicitly called out in "API Contract Preserved" (n/a, internal helper).

Test plan lifted verbatim from Parminder's [22:55] (4 cases: non-retryable → exactly one call; one-then-success → exactly two; persistent retryable → exactly two with default; `EXTRACTION_RETRIES=0` → exactly one regardless). Tests parameterized over both provider branches — running only against one is insufficient.

**Behavior change called out as the headline edge case:** today, non-retryable errors (IndexError, KeyError, JSON-decode, prompt validation) are silently swallowed and the helper returns `None`/`[]` after burning two SDK calls. After the fix they propagate. Most callers wrap broadly upstream so this is *likely* fine, but Murat must validate per-pipeline that the propagated failure mode is acceptable (visible error, no silent data loss). I considered hiding this behind a flag to preserve the silent-swallow path, but rejected — silent swallow IS the bug, not the safety net.

Other edge cases flagged: xAI parity is non-negotiable (drift = invisible incidents); `httpx.ReadTimeout`/`WriteTimeout` covered by `TimeoutException` parent (verify on next `httpx` bump); `RateLimitError` retried immediately without backoff (preserves today's behavior; backoff is a deliberate non-goal of this story); logger fields structured (`provider, attempt, total_attempts, exception_class`) — no prompt/response in the warning.

Parminder, please review for technical correctness and transition to `ready` (or push back). No open questions deferred to you on this one — the diagnosis was specific enough that I could write all ACs without ambiguity.
---

## [22:55] PARMINDER -> TEAM: [STATUS] Duplicate worthiness LLM call — root cause identified
PARMINDER: Root cause is a too-broad `except Exception: continue` retry loop in `src/services/extract_utils.py:117-146` (`_call_llm_json`). `EXTRACTION_RETRIES` defaults to `1` (config.py:194), so the loop runs up to 2 attempts. Whenever the first `client.chat.completions.create(...)` raises ANY exception — transient network blip, choices-list IndexError, response decode error, even a bug in our `_parse_json_from_text` if it ever throws — the loop re-issues a brand-new SDK call. Each new call is a fresh `client.chat.completions.create`, so the `langfuse.openai` wrapper (`langfuse/openai.py:814-881` — single observation per `wrapped()` call) emits TWO `OpenAI-generation` observations, back-to-back, identical input. Auto-generated `OpenAI-generation` name confirms both came from the SDK wrapper, not deliberate code paths. Blast radius is wide: same helper backs worthiness, extraction, sentiment, profile_extraction, graph_extraction, compaction, reconstruction (8+ call sites). One important note for the first observation: if it really raised, langfuse marks it `level=ERROR` with a `status_message`. The trace export pasted by the user didn't include those fields, so confirming `level` on the first observation in the Langfuse UI is the cheapest way to validate this diagnosis before we ship the fix. Smallest fix is two-part in `extract_utils.py`: (a) narrow the except to retryable exceptions only (timeouts / connection errors / 5xx), and (b) log a WARNING on every retry so future incidents are observable in our own logs. Recommending one P1 story scoped to `extract_utils.py` only (~15-20 LOC across both provider branches) plus a Murat unit test asserting non-retryable errors do NOT cause a second LLM call. Memory updated.
---
---

## [23:58] PARMINDER -> TEAM: [REVIEW] Story 2.2 approved → ready (with one factual correction)

PARMINDER: Reviewed Story 2.2. Disha's narrow tuple is correct; xAI parity is right; test plan is sound after one addition. Status flipped to `ready` in `status.md`. Review cycles: 1.

**Verified against `openai==1.40.0` (uv.lock + `.venv/.../openai/_exceptions.py`):**
- All six exception classes Disha named exist in the right module paths. `openai.APITimeoutError` is a subclass of `openai.APIConnectionError` (listed both for intent/readability — costs nothing). `httpx.TimeoutException` covers `ReadTimeout`/`WriteTimeout`.
- `httpx.*` entries are belt-and-suspenders (openai-python normally wraps raw httpx errors before they exit the SDK), but defensible — keep them.
- xAI uses the same `OpenAI(api_key=..., base_url=...)` client, so identical exception classes apply. Parity tuple is correct.
- Did NOT add `openai.UnprocessableEntityError` (422 client error, retry won't fix it). Did NOT add `openai.APIError` base class (would re-introduce the broad-except problem). Both correctly omitted.

**One factual correction — this is the meaningful diff:** Disha's draft framed "non-retryable errors propagate to caller" as the headline edge case. **That's wrong.** There's a SECOND `try/except Exception` at `extract_utils.py:97-220` wrapping the whole function body that logs `logger.exception(...)` (full traceback), calls `trace_error(...)`, and returns `None`. The narrow-inner-except change ONLY fixes the retry-loop double-call bug; non-retryable errors still skip the inner except → propagate out of the loop → get caught by the outer except → return None. External return contract is unchanged.

This significantly de-risks the story:
- **No caller is fragile.** Walked all 8 callers — every single one already handles `None` gracefully (`resp or default`, `if not isinstance(resp, dict): resp = {}`, or local try/except). The "validate per-pipeline" worry Disha flagged for Murat is moot.
- **No companion story needed.** Single P1 stays.
- **Test case 1's contract is now explicit:** "returns `None`" not "raises or returns None depending on contract."

Updated AC #5 to spell out the actual flow (skips inner except → propagates out of for loop → caught by outer except → logger.exception + trace_error + return None). Added Test Case 5 (`openai.BadRequestError` short-circuits to one call — guards against ever accidentally widening the tuple to base `APIError`). Rewrote the headline Edge Case to reflect "no external behavior change for callers." Added an implementation note that David needs to add `import httpx` / `import openai` at module scope since they're currently only imported locally inside the function.

**Other things I considered and rejected:**
- Narrowing the OUTER except too — TEMPTING because silent-return-None is a debugging-debt pattern, but it would force every caller to grow new error handling. That's a separate, larger refactor and NOT this bug. Flagged as a future Epic 2 candidate (revisit once Story 2.1 lands and we can see error spans natively in Langfuse).
- Splitting the caller-tolerance question into a companion story — not needed. Audit done at review; no caller is fragile.

**Smell flagged for the future, not this story:** the outer `except Exception → return None` is the second silent-failure layer. It currently buys us traceback logging + Langfuse error breadcrumb, which is real value, but the "return None on any unexpected failure" contract makes pipeline failures invisible to callers. Worth revisiting after 2.1 ships.

**Story 2.2 status: `ready`. Review cycles: 1.** Memory updated. David can pick this up when his plate clears (he's currently on 2.1).
---

## [23:50] PARMINDER -> TEAM: [REVIEW] Story 2.1 approved → ready (with corrections)
PARMINDER: Reviewed Story 2.1. Both of Disha's open assumptions answered, several reference errors caught, ACs rewritten in place, status flipped to `ready` in `status.md`. Disha's framing was solid; the file:line refs from my own [22:25] writeup were where most of the drift lived (my fault — I'll be more careful with line refs going forward).

**Disha's two open assumptions, answered authoritatively:**

1. **`compaction_graph.py:116/306` — CONFIRMED, one parent span.** Both call sites are inside the same `run_compaction_graph` invocation. Line 116 is `_cluster_memories` (called from `node_consolidate` at `:646`) and line 306 is `_consolidate_cluster` (called from `node_consolidate` at `:664`). One `compaction_run` span covers both. **Bonus find:** `run_compaction_graph` ALREADY opens a Langfuse trace at `:964` via `start_trace("compaction_job", ...)` BUT never calls `end_trace()` after `trace.update(output=...)` at `:993` — that's a span leak. Folded the fix into the AC: David rewrites `:964` to use `with langfuse_client.start_as_current_observation(...) as root_span:` wrapping `graph.compile().invoke(initial)`, renames to `compaction_run`, and the `with` block guarantees clean close.

2. **`episodic_memory.py:119/236` — CONFIRMED two distinct paths, but Disha's labels are SWAPPED.** Line 119 is in `_store_in_chroma` (write path, called from `store_memory` at `:44`) → span `episodic_store`. Line 236 is in `_semantic_search` (read path, called from `retrieve_memories` at `:225`) → span `episodic_query`. ACs corrected.

**Other reference corrections that landed in the rewrite:**

- `forget.py:66` is **dead code** — that line is in `forget.simple_deduplicate` (line 45), which has no callers. The live `forget` entry is `run_compaction_for_user` at `:101`, which delegates straight to `run_compaction_graph` and is therefore covered by `compaction_run`. Dropped the standalone `forget_memory` AC entirely.
- `compaction_ops.py:81` is **inside a docstring**. Real `generate_embedding(...)` is at `:137`, inside `simple_deduplicate`. That function is only called from `compaction_graph.py:557` (inside `node_dedup`), so it always runs nested under `compaction_run`. Kept the AC as a defensive nested-detection guard since `simple_deduplicate` is module-public and could theoretically be called directly.
- **Wrap target is the enclosing public method, not the literal `generate_embedding(...)` line.** For each service, called out the right entry point: `search_memories` at `retrieval.py:70`, `retrieve_memories` at `hybrid_retrieval.py:97`, `store_memory`/`_semantic_search` in `episodic_memory.py`, `_store_in_chroma` in `emotional_memory.py`. David wraps at the function entry.
- **`emotional_memory.py:196` is a WRITE path** (inside `_store_in_chroma`), not a query. Renamed Disha's `emotional_query` → `emotional_store`. There is no separate emotional read-path embedding in that file; semantic emotional search routes through `hybrid_retrieval`.

**Two technical things worth flagging that the original ACs got wrong:**

1. **Nested-detection SDK call.** Disha listed `langfuse_client.get_current_span()` — that method **does not exist** in `langfuse-python==3.14.2`. Verified: the right call is `langfuse_client.get_current_trace_id()` (returns `Optional[str]`, `None` when no span is active) at `.venv/.../langfuse/_client/client.py:2328`. Updated everywhere in ACs and edge cases.

2. **Async wrap is a non-issue here.** Disha flagged `async def` as an open concern. Verified: every wrap target in this story is sync `def` (the `/v1/memories/direct` handler at `routers/memories.py:321`, all the service methods). `start_as_current_observation` is a sync context manager. `with`, not `async with`, everywhere. Tightened the edge case from "confirm with Parminder" to "sync-only, future async is a separate story."

**Other tightenings:**

- Added explicit AC: existing `unified_ingestion_graph.py:1428-1452` block must NOT be modified, removed, or renamed by this story.
- Added AC clarifying `hybrid_retrieval.retrieve_memories:109` is the one place where existing `tracing.start_span(...)` gets rewritten to `start_as_current_observation` — because that's the OUTERMOST wrap for that entry point. Other nested `start_span` usage in `compaction_graph.node_*` stays as-is and naturally becomes child-of-`compaction_run` once the root exists.
- Route correction: direct-ingest endpoint is `POST /v1/memories/direct`, not `/v1/memories` (Disha's body had it as the latter — router prefix `/v1/memories` + route `/direct`).
- `user_id` flow: verified directly available at every call site (`body.user_id`, `query.user_id`, `memory.user_id`, function arg). No fallback needed; encoded in review notes.

**Risk to flag for David:**
The `compaction_run` change is the biggest piece — switching from the existing `start_trace`/`trace.update` pattern (which leaks) to a `with` context manager. He should make sure the existing nested `start_span` calls inside `node_*` continue to work (they should — they read from `_current_root_span` contextvar which `start_trace` populates; the `with` rewrite must populate the same contextvar OR migrate the nested spans too). I left the implementation choice to him but called it out in the AC. If David hits friction, I'll consult.

**Story 2.1 status: `ready`. Review cycles: 1.** Status table updated. Disha unblocked to keep drafting; David can pick this up whenever the user lifts the implementation hold.

Memory note for myself coming out of this review: when I publish a diagnosis with file:line refs in a hurry (like my [22:25]), I should walk those refs one more time before Disha consumes them. Three of the eight refs were imprecise (forget.py:66 → dead, compaction_ops.py:81 → docstring, episodic 119 vs 236 → labels swappable). Cheap to fix at review time, but ideally caught before Disha drafts off them.
