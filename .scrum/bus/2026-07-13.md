# Bus — 2026-07-13

## [20:40] JOHN -> ALL: [STATUS] Scrum dir migrated
JOHN: Migrated `.claude/scrum/` → `.scrum/` (new layout) and renamed legacy memory files (fenny→john, disha→penny, parminder→aria, david→dave, harpreet→remy, murat→tess). Bus files >7 days rotated out.
---

## [20:41] JOHN -> ARIA: [TASK] Verify PG-saturation incident diagnosis + review fix plan
JOHN: Production incident 2026-07-12 ~20:00 — TimescaleDB hit max_connections (100), saturated ~24h ("FATAL: too many clients"), locked out even admin psql. Stack was recreated 2026-07-13 ~20:12 so old container logs are gone. Root-cause investigation (by root agent) concluded:
1. PRIMARY: `_temporal_retrieval` (src/services/hybrid_retrieval.py:423) checks out a pooled conn, never releases on any path. Triggered via POST /v1/narrative with start_time+end_time → reconstruction.build_narrative → retrieve_memories (time_range set).
2. Secondary: `_store_in_timescale` (src/services/episodic_memory.py ~96-140) — rollback before release, not in finally; leaks on broken connections.
3. Systemic: `release_timescale_conn` (src/dependencies/timescale.py:78-83) — rollback+putconn share one try; broken conn skips putconn.
4. Capacity: 10 uvicorn workers × pool max_size=10 = exactly 100 = max_connections; app connects as postgres superuser (reserved slots gave no protection).
5. Rejected earlier theory: APScheduler job duplication (add_job already has stable id + max_instances=1; the "duplication" was 10 workers × 1 scheduler each, log lines double-printed by duplicate handlers src/app.py:391-411).
Task: independently verify every claim against code; review proposed fixes (finally-release in _temporal_retrieval; harden release helper; finally in _store_in_timescale; pool/max_connections headroom; non-superuser role; single-worker scheduler; dedupe log handlers). Verdict: approve/amend/reject.
---
## [21:05] ARIA -> JOHN: [REVIEW] Incident diagnosis VERIFIED — fix plan approved with amendments
ARIA: All 5 claims verified against code (hybrid_retrieval.py:423 leak confirmed; release helper flaw elevated to co-P0 multiplier; capacity math exact: 10 workers × max_size 10 = 100 = max_connections; superuser DSN confirmed; APScheduler theory correctly rejected). Amendments: hotfix = Fixes 1+2+3 in ONE commit (correctness-only, zero happy-path change); pool max_size 10→5 as separate revertable PR; non-superuser role + hygiene items as follow-up stories; do NOT raise max_connections. Tests: (a) unit — rollback raises → putconn still called; (b) spy — release called exactly once on happy+error paths in _temporal_retrieval and _store_in_timescale; (c) Tess owns incident-repro integration test vs /v1/narrative. Also: surface pool.get_stats() in /health/full for post-deploy observation. psycopg_pool 3.2.1 putconn confirmed safe on broken conns.
---

## [21:06] JOHN -> DAVE: [TASK] Story H-1: PG connection-leak hotfix (Fixes 1+2+3 + tests)
JOHN: Implement Aria-approved hotfix on branch story/H-1-conn-leak-hotfix. Scope: (1) try/finally release in _temporal_retrieval; (2) release_timescale_conn: rollback in own try/except, putconn always runs; (3) _store_in_timescale → try/finally; (4) pool.get_stats() in /health/full; (5) Aria's tests a+b. Run make lint + targeted tests. Commit locally; do not push.
---

## [20:56] DAVE -> REMY: [STATUS] Story H-1 → review — PG connection-leak hotfix landed
DAVE: All 4 fixes implemented on branch `story/H-1-conn-leak-hotfix` (commit 7d414c6), one commit, correctness-only, zero happy-path change.
- Fix 1 (hybrid_retrieval.py `_temporal_retrieval`): added `finally: if conn: release_timescale_conn(conn)` — matches sibling `_emotional_retrieval` style exactly. Conn was never released before.
- Fix 2 (timescale.py `release_timescale_conn`): split rollback and putconn into separate try/except so a broken conn (rollback raises) STILL putconns. Kept print() logging style; updated docstring to state the new guarantee.
- Fix 3 (episodic_memory.py `_store_in_timescale`): moved release into `finally`, matching `_time_range_search`/`get_memory_by_id`/`delete_memory`. Kept rollback-in-except + re-raise (contract with store_memory preserved).
- Fix 4 (app.py `/health/full`): added `checks["timescale_pool"] = pool.get_stats()` after the conn release, guarded by try/except; informational only, never affects overall status. Did NOT touch the endpoint's own try/finally (that's H-4).
- Tests: new `tests/unit/test_conn_leak_hotfix.py`, 5 tests. Test A (Fix 2 regression) verified to FAIL against pre-fix code (putconn called 0 times) and pass after. Tests B/C spy on release: exactly once on happy + error paths for both fixed functions.
- Results: new file 5/5 passed; touched-module suite (test_app_health, test_tracing_root_span, test_app_store, test_conn_leak_hotfix) 15/15 passed. `make lint` clean. Also manually confirmed /health/full surfaces the pool stats dict (and None when no pool, no crash).
- Deviations: none from spec. Committed only my 5 code/test files — left the pre-existing `.claude/scrum/`→`.scrum/` migration churn out of the commit. Not pushed, no PR (per instructions).
---

## [21:06] Tess -> team: [STATUS] Story H-1 - PASSED (ship-ready pending deploy decision)
Tess: Test gate complete. Verdict: PASS. H-1 -> done.

### Verdict
ALL PASS on everything I could safely run against this box. The live docker
stack here is prod on the OLD (leaky) image, so I deliberately did NOT run the
app-based integration tests — see "Deferred" below. That's a containment call,
not a coverage gap in H-1: the three release fixes are fully unit-covered.

### What I tested (vs H-1's ACs)
- AC "release_timescale_conn always putconns even when rollback raises" (Fix 2):
  test_release_still_putconn_when_rollback_raises PASSED. Mocks a conn whose
  rollback() raises; asserts putconn(conn) still called exactly once. This is
  the incident pin.
- AC "_temporal_retrieval releases conn on every path" (Fix 1):
  happy + error path tests PASSED — release called exactly once each, commit on
  happy path, [] returned (error swallowed + logged) on error path.
- AC "_store_in_timescale releases via finally" (Fix 3):
  happy + error path tests PASSED — release once each; error path rolls back and
  re-raises "insert boom".
- AC "/health/full exposes pool.get_stats()": confirmed by read of src/app.py:997
  (checks["timescale_pool"] = pool.get_stats() if pool else None, try/except-
  guarded). NOT exercised by any unit test with a real pool — post-deploy only.

### Ran (exact)
- Full unit suite (`pytest tests/unit -q`): 540 passed, 1 skipped, 41 warnings, 9.09s.
  Skip = tests/unit/test_profile_api.py:656 "Requires actual database connection"
  — pre-existing, unrelated to H-1. Whole suite because the touched modules are
  import-shared broadly; no regressions.
- Hotfix suite `-v` (tests/unit/test_conn_leak_hotfix.py): 5 passed, 0.57s.
- Hermetic integration (tests/integration/test_retrieve_filters.py): 6 passed, 0.84s.
  Safe because it never imports src.app — it stubs the collection at
  src.services.retrieval._get_collection and calls search_memories directly.

### Deferred (NOT run) and why
- tests/integration/{test_forget_api,test_intents_api,test_direct_memory_api,
  test_chroma_wrapper}.py all do `from src.app import app` + TestClient(app),
  which runs the REAL lifespan. Two live-prod hazards confirmed by code read:
  (1) lifespan calls client.list_collections() against Chroma on localhost:8000
  = live prod Chroma (read); (2) lifespan calls _start_scheduler(), and
  is_scheduled_maintenance_enabled() resolves True by default (unset in this
  shell) -> starts APScheduler running _run_ttl_sweep (interval) +
  _run_daily_compaction, both of which DELETE from the live prod stack.
  Per gate scope (no writes to live prod), I held these. Ports 8080/8000/5432/
  6379 are all OPEN, so the tests' own reachability guards would NOT skip —
  they'd hit prod. This is CI/hermetic-env work, not this-box work.

### Coverage gaps (for the record)
- H-1 unit tests prove the release semantics at the function boundary via spies.
  They do NOT prove: real pool exhaustion + recovery under concurrent load, nor
  that live /health/full carries real timescale_pool.get_stats() numbers (the
  one existing health test stubs the pool to None). Both are post-deploy-only.
- These are exactly story H-5 (mine), to run after deploy.

### H-5 test plan (post-deploy, incident-repro)
1. Spin an isolated app instance with a tiny Timescale pool (min=1, max=2) against
   a NON-prod DB; assert baseline pool.get_stats() is flat.
2. Fire N concurrent time-ranged POST /v1/narrative requests (the path through
   _temporal_retrieval) exceeding max_size; assert NO PoolTimeout/pool-exhaustion
   error and every request returns.
3. After the burst settles, assert pool stats return to baseline (checked-out
   count flat, no monotonic climb) — the direct regression for the 2026-07-12 leak.
4. Force the failure path (inject a raising query / broken conn) under concurrency;
   assert connections still return to the pool (Fix 1/2/3 hold under contention).
5. GET /health/full and assert checks["timescale_pool"] is a real stats dict
   (keys present, integer counts) — the piece I could only read, not run, today.
---
